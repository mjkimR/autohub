name: autohub
version: 0.1.0
targets: [claude, codex]
dependencies:
  apm:
    # Match the shared frontend lint policy; Python runtime packages keep their own pin.
    - git: mjkimR/app-common
      path: agents
      ref: 915f8f512b7d590a999291cbe465fa16f2ba6f1c
      skills: [app-common]
